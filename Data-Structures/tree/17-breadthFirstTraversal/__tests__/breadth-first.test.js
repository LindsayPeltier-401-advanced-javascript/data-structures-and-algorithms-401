'use strict';

const Tree = require('../breadth-first.js');

// describe('binary tree', () => {
//   test('it should not be empty', () => {
//     const input = new Tree(2,
//       new Tree(7, null, null),
//       new Tree(5,
//         new Tree(2, null, null),
//         new Tree(6,
//           new Tree(9, null, null),
//           new Tree(5,
//             new Tree(11, null, null),
//             new Tree(4, null)))));

//     const output = console.log(input);
//     expect(output).toNotEqual(null);
//   });
// });